# Madrid Routing App — Frontend

React + Vite + Leaflet + Tailwind CSS frontend for the Madrid Routing App.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the development server:
   ```bash
   npm run dev
   ```

The app will be available at `http://localhost:5173`.
